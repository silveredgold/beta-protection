import{r as a,o as n,c as i,a as t,b as s,F as r,e as d,d as e}from"./app.7e250e46.js";import{_ as c}from"./plugin-vue_export-helper.21dcd24c.js";const l={},h=d(`<h2 id="build" tabindex="-1"><a class="header-anchor" href="#build" aria-hidden="true">#</a> Build</h2><p>Once you&#39;ve got the repo cloned, you should be able to just do the usual install/build combo:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code><span class="token function">npm</span> i
<span class="token function">npm</span> run build
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>That will build a production build of the extension into the <code>dist/</code> directory. You can load that into the browser with the usual <code>Load Unpacked</code> option, but if you&#39;re going to be working on the extension that&#39;s unpleasant. You can instead run <code>npm run serve</code> to run a dev server. Once the initial build is done, use Load Unpacked from your browser of choice, and now webpack (via the Vue CLI) will watch for changes, rebuild on change and (theoretically) trigger a reload in your browser when required.</p><h2 id="documentation" tabindex="-1"><a class="header-anchor" href="#documentation" aria-hidden="true">#</a> Documentation</h2><p>The docs live separately in the <code>docs/</code> folder, with their own dependencies and their own <code>docs:dev</code> and <code>docs:build</code> build scripts. The docs are automatically built on change with GitHub Actions, and automatically published from the main branch.</p><h2 id="implementing-an-additional-backend" tabindex="-1"><a class="header-anchor" href="#implementing-an-additional-backend" aria-hidden="true">#</a> Implementing an additional backend</h2><blockquote><p>Due to security limitations in the browser, it&#39;s borderline impossible to ship backends separately, so at present any additional backend support will have to be contributed directly to this repo.</p></blockquote><p>At it&#39;s most basic, a backend consists of two parts: an implementation of <code>ICensorBackend</code> to handle the &quot;transport&quot; between Beta Protection and the backend, and an implementation of <code>IBackendProvider&lt;ICensorBackend&gt;</code> for that backend to &quot;wire up&quot; the backend with Beta Protection&#39;s events and components.</p><h4 id="resources" tabindex="-1"><a class="header-anchor" href="#resources" aria-hidden="true">#</a> Resources</h4>`,10),u=e("The "),p=t("code",null,"ICensorBackend",-1),b=e(" interface has some reasonably okay documentation "),m={href:"https://silveredgold.github.io/beta-shared/interfaces/transport.ICensorBackend",target:"_blank",rel:"noopener noreferrer"},f=e("available here"),_=e(" that should help with that part. As for the provider, that will vary heavily from backend to backend, but you can see the existing examples for Beta Censoring "),k=t("a",{href:"#"},"here",-1),g=e(" and for Beta Safety in this repo at "),v=t("code",null,"src/transport/beta-safety/provider.ts",-1),w=e(".");function y(x,B){const o=a("ExternalLinkIcon");return n(),i(r,null,[h,t("p",null,[u,p,b,t("a",m,[f,s(o)]),_,k,g,v,w])],64)}var C=c(l,[["render",y]]);export{C as default};

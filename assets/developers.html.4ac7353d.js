import{e}from"./app.23e9a5e8.js";import{_ as n}from"./plugin-vue_export-helper.21dcd24c.js";const o={},a=e(`<h2 id="build" tabindex="-1"><a class="header-anchor" href="#build" aria-hidden="true">#</a> Build</h2><p>Once you&#39;ve got the repo cloned, you should be able to just do the usual install/build combo:</p><div class="language-bash ext-sh line-numbers-mode"><pre class="language-bash"><code><span class="token function">npm</span> i
<span class="token function">npm</span> run build
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>That will build a production build of the extension into the <code>dist/</code> directory. You can load that into the browser with the usual <code>Load Unpacked</code> option, but if you&#39;re going to be working on the extension that&#39;s unpleasant. You can instead run <code>npm run serve</code> to run a dev server. Once the initial build is done, use Load Unpacked from your browser of choice, and now webpack (via the Vue CLI) will watch for changes, rebuild on change and (theoretically) trigger a reload in your browser when required.</p><h2 id="documentation" tabindex="-1"><a class="header-anchor" href="#documentation" aria-hidden="true">#</a> Documentation</h2><p>The docs live separately in the <code>docs/</code> folder, with their own dependencies and their own <code>docs:dev</code> and <code>docs:build</code> build scripts. The docs are automatically built on change with GitHub Actions, and automatically published from the main branch.</p>`,6);function t(i,d){return a}var c=n(o,[["render",t]]);export{c as default};
